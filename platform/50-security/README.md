# Stage 50 — Security

Enables Microsoft Defender for Cloud plans (CSPM + workload protection) and
provides hooks to onboard a CNAPP layer. The CNAPP vendor and connector config
are kept in `_private/` — the public module only exposes generic inputs.
