# `modules/private-endpoint`

Creates a private endpoint for a target PaaS resource and wires it to the
correct hub Private DNS zone.

> Skeleton placeholder. Inputs will include: `target_resource_id`,
> `subresource`, `subnet_id`, `private_dns_zone_id`.
