# `modules/udr`

Route table that forces `0.0.0.0/0` (and optional on-prem prefixes) to the hub
Azure Firewall, then associates it to spoke subnets.

> Skeleton placeholder. Inputs will include: `firewall_private_ip`,
> `extra_routes`, `subnet_ids`.
